# Private repair acceptance

Main tested viewer source `d930324325325638d15d0d6ba29299d413c7b21d` with the complete isolated Web profile. Existing alpha document automation remained false/false after changing both defaults to true. A newly opened beta document initialized true/true. Setting beta to false/false survived cold reload. The default checkboxes had equal Y positions and neither inheritance button existed. Screenshots `repair-defaults-horizontal.png` and `repair-controls-wide.png` are retained under `/root/dsh-resource-workbench-candidate`.

The integrated resource-links plugin opened message paths in CodeMirror and an SVG in the image handler. Directory links opened the independent file manager. This integration transfers Chat path recognition and open policy to resource-links; the manager retains filesystem access and the Files selector. No further viewer implementation changed after its 84-test, typecheck and build evidence.

All observations used a private Home and candidate plugin worktrees. The managed profile, original linked viewer checkout and service remain unchanged. The revision has private-browser evidence, not managed activation, user visual acceptance or an accepted realization lock.
