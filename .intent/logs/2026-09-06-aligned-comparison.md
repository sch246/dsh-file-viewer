# Aligned comparison and compact preferences

The user requested paired Base/current line numbers, shared Base-aligned geometry across Local and Source, linked scrolling, editable Local text, read-only Source text, nonselectable deleted baseline rows, and stronger inserted-fragment backgrounds. The user also requested retained toolbar expansion and hover-revealed default checkboxes immediately left of the current automation/line-number checkbox. Defaults continue to initialize only new documents or presentations. Installation and Web restart were explicitly authorized for this revision.

The target removes independent per-pane comparison geometry and the hidden duplicate Local view. One local editor owns document editing and undo in both modes; a derived alignment model owns row placement for both panes. The existing handler-state slot retains presentation preferences and opaque editor state. The separate More/defaults section and hover-only expansion state are removed.

Two focused control checks passed for retained expansion, current line-number toggling and default initialization across view remounts. Worker checks passed: eight editor-state cases, two alignment cases including 512 text combinations, and one CodeMirror editing/mode-switch case. The combined owned TypeScript and browser bundle build passed after correcting optional comparison arguments; the final editor-only build includes numbering and wrap-metric fixes.

The existing isolated Web at loopback port 35073 served the candidate. A memory source with 70 baseline lines exercised unequal insertions, local replacement with a long wrapped line, and independent source replacement. Browser measurement initially found a 454.28125px offset caused by differing editor and measurement padding. Both now use 2px inline padding and fractional content width. Repeating the same probe found 63 common rendered rows with zero position/height difference; the wrapped row measured 473.28125px in both panes. Linked scroll positions were 700/700. Real keyboard edits reached shared Local state; Source rejected typed input. Select-all followed by the editor copy event produced exactly the current Local document, excluding deletion widgets and padding. Numbering hid both comparison gutters, toolbar expansion survived mouse leave, and the revealed default checkbox remained pointer-reachable. No browser page or console errors were observed. Live activation remains pending.
