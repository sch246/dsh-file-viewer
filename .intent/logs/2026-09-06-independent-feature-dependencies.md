# Independent feature dependency target

The September 6, 2026 user instruction “可联动不意味着必依赖” separates optional cooperation from installation requirements. The [latest STATE target](../state/STATE.md#latest-dependency-target--migration-pending) owns the dependency table and pending ownership transfers. It supersedes earlier target statements while preserving the installed graph as current implementation evidence.

This isolated documentation change updates STATE before contributor instructions. It changes no runtime, package manifest, lockfile, Host adaptation, profile or service. Existing deployment logs and frozen records remain untouched. Shared-provider extraction, source/configuration migration, common opening and independent artifact consumption still require implementation; no combination or browser behavior is claimed here.

Validation: `git diff --check` and a local Markdown target/fragment check pass for the three changed documents. No runtime tests or installation commands were run for this documentation-only change.
