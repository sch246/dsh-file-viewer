# Shared editor settings and Save As

The user selected one Harness user configuration shared across browsers, with missing-font fallback, good compatibility with existing TextMate themes and Save As followed by switching the current editor path. Fully loaded unwritable resources remain locally editable; only original-path publication depends on write support. The user explicitly requested direct primary-checkout work, no new worktrees and no tests for this delivery.

The viewer registers the `file-viewer` namespace with the existing Harness settings provider. Font enumeration belongs to the browser machine and requires an explicit action; manual family names remain available. Theme files are read on the Host and retain TextMate scope selectors through Shiki/Oniguruma incremental line parsing. JSONC, plist, relative includes and token-color files are supported. Settings parsing is cached by file identity; mounted editors refresh settings on browser focus or opening controls. No second settings document or browser-local appearance owner remains.

Save As uses independently published user-files 0.1.7 preparation/publication methods, with no dependency on manager or Links. Existing destination replacement needs confirmation and the prepared exact revision; new files use no-clobber publication. The workbench rejects unsaved destination documents/drafts and retains later edits. If the destination is edited while the copy is written, both local documents remain and the current tab does not switch. Source loading approval still governs browser content downloads; preparing an explicit destination can read its revision on the server without downloading it to the editor. Existing patch-based ordinary Save is unchanged.

Viewer/editor 0.1.2 and optional languages 0.1.1 were built against `/root/deepseek-harness`; user-files 0.1.7 was built first to regenerate Remote declarations. The owned builds completed successfully. No tests, browser automation or visual acceptance ran. Existing tests covering retired browser preferences and legacy grammar descriptors were not updated or claimed to pass. Local activation and exact source/build evidence belong in `/root/.local/state/dsh-maintenance/2026-09-08-save-as-settings/`.
