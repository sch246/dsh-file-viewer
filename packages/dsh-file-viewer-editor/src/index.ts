/** Node half for the browser-only editor graph row. */
export const name = 'file-viewer-editor'

/** Browser code is loaded through the package's client graph row. */
export function apply(): void {}
