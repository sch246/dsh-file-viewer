/** Node entry for the optional browser language pack. */
export const name = 'file-viewer-languages'

/** Language registration belongs to the package's client plugin. */
export function apply(): void {}
