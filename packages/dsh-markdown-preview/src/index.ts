/** Host entry; Markdown rendering is contributed by the Client plugin. */
export const name = 'markdown-preview'
/** The Bundle registers the browser contribution. */
export function apply(): void {}
